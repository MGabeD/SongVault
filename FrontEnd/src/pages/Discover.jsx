import { Typography } from '@material-ui/core'
import React from 'react'

import SearchBar from '../Components/SearchBar/SearchBar'

const Discover = () => {
    return (
        <div style={{background: 'black', height: '80vh'}}>
            <SearchBar/>
        </div>
    )
}

export default Discover